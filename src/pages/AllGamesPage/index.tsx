import React, { FC } from "react";
import AllGamesList from "../../modules/AllGamesList";

const Index: FC = () => {
  return (
    <div>
      <AllGamesList />
    </div>
  );
};

export default Index;
