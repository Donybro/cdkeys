import React, { FC } from "react";
import { Layout } from "antd";
const { Header } = Layout;

const Index: FC = () => {
  return <Header style={{ padding: 0, background: "#fff" }}></Header>;
};

export default Index;
